name = "tiktok-download-lab"
compatibility_date = "2024-05-12"

# Karena kita deploy pake Cloudflare Pages, wrangler setup ini opsional
# Tapi berguna kalau mau ngetes lokal pake wrangler: `npx wrangler pages dev dist`
[site]
bucket = "./dist"

[env.production.vars]
# JANGAN LUPA SET GEMINI_API_KEY di dashboard Cloudflare Pages kamu!
# GEMINI_API_KEY = "isikan-di-dashboard"
